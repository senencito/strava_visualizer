# ══════════════════════════════════════════════════
# Senén Strava Visualizer — Environment Variables
# Copy this to .env for local dev, set these in
# Railway dashboard for production
# ══════════════════════════════════════════════════

# Strava API — get from strava.com/settings/api
STRAVA_CLIENT_ID=your_client_id_here
STRAVA_CLIENT_SECRET=your_client_secret_here

# Where Strava redirects after OAuth
# Local: http://localhost:3000/auth/callback
# Production: https://your-app.railway.app/auth/callback
STRAVA_REDIRECT_URI=http://localhost:3000/auth/callback

# Session secret — generate a random string, e.g.:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change_me_to_a_random_string

# PostgreSQL — Railway sets this automatically in production
# For local dev, set up a local Postgres and paste the connection string
DATABASE_URL=postgresql://user:password@localhost:5432/senen_viz

# Environment
NODE_ENV=development
PORT=3000

# Admin page password (for /admin?pass=YOUR_PASSWORD)
ADMIN_PASSWORD=SemeOlvido1!
